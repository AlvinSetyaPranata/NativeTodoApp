import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  title: {
    color: "black",
    fontSize: 30,
    fontWeight: "500"
  },

  wrapper: {
    alignItems: "center",
    paddingTop: 40
  },

  header: {
    fontWeight: "bold",
    fontSize: 40,
    textAlign: "center",
    marginBottom: 50
  },

  todoContainer: {
    rowGap: 30,
  }

});



export default style